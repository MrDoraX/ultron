# Optional cloud boost (offline-first works without these)
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=

# PC Electron bridge port
ULTRON_BRIDGE_PORT=8765

# Optional lab relay — prefer Tailscale IP/MagicDNS in the phone Panel instead
# VITE_ULTRON_RELAY_URL=ws://127.0.0.1:8787
